/**
 * Context Index
 * 
 * Barrel exports for all context providers.
 * 
 * @module context
 */

export { 
  AuthContext, 
  AuthProvider, 
  useAuth,
  type AuthState,
  type OTPState,
  type AuthContextValue,
} from './AuthContext';
